"""Copy this file to local_settings.py (same folder) and fill in your demo
account's credentials. local_settings.py is git-ignored, so it never gets
committed."""

MT5_LOGIN = 12345678
MT5_PASSWORD = "your-password"
MT5_SERVER = "Broker-Demo"
MT5_PATH = None  # only needed if MT5 isn't installed in the default location
